/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // Optional: Change this to your repository name
  basePath: "/Halloween-Challenge-Perfect-Landing",
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
